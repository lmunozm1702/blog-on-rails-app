<% posts.each do |post| %>
  <div>
    <div>Post #<%= post.id %> - <%= post.title %></div>
    <div><%= post.text %></div>
    <div>Comments: <%= post.comments_counter || 0 %>, Likes: <%= post.likes_counter || 0 %> </div>
  </div>
<% end %>
