<!DOCTYPE html>
<html>
  <head>
    <title>BlogOnRailsApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <div class="container p-4">
      <% if user_signed_in? %>
        <div class="text-end">
          <%= button_to "logout", destroy_user_session_path, method: :delete, class: 'badge bg-secondary'  %>
        </div>
      <% end %>
      <%= render partial: 'layouts/notice_flash_message', locals: { notice: notice } if notice %>
      <%= render partial: 'layouts/alert_flash_message', locals: { notice: notice } if alert %>
      <%= yield %>
    </div>
  </body>
</html>
